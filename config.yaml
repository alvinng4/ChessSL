engine:
  model_path: "/Users/alvinng/Desktop/cuhk/Year 4/PHYS4061/ChessSL/run0_model_epoch70.pt"
  device: "cpu"                       # "cpu" or "cuda:0"
  default_num_nodes: 1024               # Number of nodes if it is not specified in the command "go"
  mcts_batch_size: 32
  mcts_batches_early_stop: 2048         # Early stop if the batch is not filled after this iterations
  c_puct: 0.5
  c_fpu: 0.2
  virtual_loss: 3.0
  
training:
  create_new_model: True
  current_epochs: 0
  model_dir: "models"
  load_model_name: "model_epoch0.pt"
  training_data_dir: "/home/alvin/chessSL_training_data"
  device: "cuda:0"                       # "cpu" or "cuda:0"
  batch_size: 512
  data_loader_num_workers: 4
  data_loader_pin_memory: True
  num_epochs: 100
  learning_rate: 0.001
  policy_weight: 1.0
  winner_weight: 1.0
  mlh_weight: 0.0005
  log_dir: "chessSL_training_logs"
  log_interval: 200                      # Log training status every log_interval batches
  save_interval: 10                      # Save model every save_interval epochs


